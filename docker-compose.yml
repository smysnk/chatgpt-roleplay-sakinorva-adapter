services:
  web:
    image: nextjs-roleplay:latest
    container_name: nextjs-roleplay
    env_file:
      - .env
    volumes:
      - ./.env:/app/.env:ro
    ports:
      - "${HOST_PORT:-3000}:3000"
    restart: unless-stopped
